import { AppProject } from './types';

export const FEATURE_FLAGS = {
  VITE_LAUNCH_MODE: 'week1', // set to 'standard' to revert
  VITE_FEATURE_LAUNCH_W1: true,
  VITE_FEATURE_DISCOVERY_SURFACES: true,
  VITE_FEATURE_CREDIBILITY_VISUALS: true,
  VITE_FEATURE_PUBLIC_BIO: true,
  VITE_FEATURE_LEADERBOARD: true,
  VITE_FEATURE_EMBEDS: true
};

export const MOCK_APPS: AppProject[] = [];
